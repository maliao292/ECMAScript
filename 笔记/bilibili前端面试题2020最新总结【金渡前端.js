/*  todo
let var 区别
 1. 不存在变量提升
 2. 同一作用域下不能重复定义同一名称
 3. 有着严格的块级作用域
 
 const:
 定义的常量只读，定义时必须赋值
 
 
 
 set map 
 set: 类似数组
	支持链式调用
	const s = new Set()
	s.add(1).add(2)
	
 map：const m = new Map()
		m.set(key,value)
 for(let [key,value] of m){
	 
 }
 */
